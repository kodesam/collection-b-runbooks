To create a file in Linux, you can use the `touch` command. Here's an example:

```shell
touch filename.txt
```

This command will create a new empty file named "filename.txt" in the current directory. If the file already exists, it will update its access and modification timestamps.